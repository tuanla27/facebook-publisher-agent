{
  "servers": [
    {
      "name": "asset-storage",
      "purpose": "Provide scoped image references and trusted asset metadata",
      "tools": ["get_asset_for_analysis"],
      "model_access": "read-only"
    },
    {
      "name": "brand-knowledge",
      "purpose": "Read approved brand rules and source references",
      "tools": ["get_approved_brand_reference"],
      "model_access": "read-only"
    },
    {
      "name": "approval",
      "purpose": "Create and read human review tasks",
      "tools": ["create_review_task", "get_review_status"],
      "model_access": "create-and-read-only"
    },
    {
      "name": "meta-page-publisher",
      "purpose": "Publish an already approved Page job",
      "tools": ["publish_approved_post"],
      "model_access": "backend-guarded",
      "enabled_by_default": false
    }
  ],
  "forbidden_tools": [
    "publish_post(page_id, caption, asset_url)",
    "publish_arbitrary_text",
    "get_page_access_token"
  ]
}
